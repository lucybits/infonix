/**
 * este es el orquestador principal de la vista de inicio
 * la responsabilidad de este componente es exclusivamente la composición 
 * de los bloques atómicos de la interfaz
 */

export default function HomePage() {
  return (
    <main className="flex flex-col min-h-screen">
      
      {/* el Hero y el resto de secciones se inyectan como componentes modulares
        la disposición (layout) se gestiona aquí mediante flexbox 
        para que el flujo vertical sea consistente
      */}
      
    </main>
  );
}